import { ErrorValidatorDirective } from './error-validator.directive';

describe('ErrorValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
